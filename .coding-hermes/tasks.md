
## Dogfood Findings (2026-09-01)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"HTTP server: Next.js 16 standalone app booted through custom-server.js (federation-aware wrapper) — serves the OpenAI-compatible /v1 API (chat/completions, /v1/messages, /v1/models) plus the management dashboard at /dashboard on port 20128 (PORT env overrides); a CLI launcher package

- [P0] Silent DATA_DIR fallback touches the real instance DB — Doc-faithful `cp .env.example .env` points DATA_DIR at /var/lib/9router, which is unwritable → app silently falls back to ~/.9router with no warning; first dev boot opened an existing install's DB (shm/wal touched, data.sqlite mtime unchanged) — isolation is undocumented and any real user with an existing install risks cross-instance data bleed.
- [P0] Federation compose crash-loops on doc-faithful .env — `docker compose -f docker-compose.federation.yml up` with the documented `cp .env.example .env` steps FATALs on placeholder secrets, crash-looping all 3 containers; remedy exists only in a compose-file comment + FEDERATION.md §6.1, while the README compose section is silent — the fork's headline deployment path fails for everyone following the docs.
- [P1] npm run cli:pack fails out of the box (MODULE_NOT_FOUND: esbuild) — cli/ deps are never installed and no doc says to run `npm install` inside cli/ (cli/README only covers global npm install); a documented command in the promise's run_commands list dies immediately.
- [P1] Standalone docker compose aborts on hardcoded headroom 8787 — `docker compose up -d` cannot start: headroom container can't bind 0.0.0.0:8787 (occupied on this host); port is hardcoded with no documented override or exclusion, leaving the stack Created — host-specific but unfixable without reading source.
- [P1] FEDERATION_MODE ignores REQUIRE_API_KEY=false (bare 401) — Federation containers return unauthenticated 401 on /v1/models despite REQUIRE_API_KEY=false because roleGuard precedes the API-key check — the only 401 emitter in the /v1 path; bare error, no hint, not covered in README quick-start, so a user following config docs appears locked out.

## Dogfood Findings (2026-09-04)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"HTTP server: a Next.js 16 app (plain JS ESM) booted through custom-server.js, exposing an OpenAI-compatible /v1 API (chat/completions, messages, models, responses, audio, embeddings, images) plus an Anthropic-compatible POST /v1/messages and a web dashboard at /dashboard (port 20128,

- [P1] Documented regression gate fails: 12 pass->fail regressions — verify-no-regression.mjs (the project's own judge for 'not all green by design') reports 12 pass->fail regressions (rtk.test.js x8, antigravity-mitm, kiro-model-slots) — the gate is NOT clean, so the 
- [P1] DATA_DIR silently falls back to ~/.9router and touches real instance state — cp .env.example .env yields DATA_DIR=/var/lib/9router, which a non-root user cannot create; the app silently falls back to ~/.9router with no warning, so a new user can unknowingly mutate a pre-existi
- [P1] Production server killed silently mid-run (exit 137, no crash log) — npm run start was SIGKILLed with only 'Killed' in output and no crash log; a real user's long-running gateway can die mid-coding with no diagnostic trail, and the README offers no recovery guidance.
- [P2] Port-collision footguns: silent wrong-server hits and hardcoded 8787 — Port 20128 was already occupied by a pre-existing instance, so the README's default command silently hit the wrong server; docker compose up -d aborts because the headroom image hardcodes 0.0.0.0:8787
- [P2] CLI packaging docs gap and confusing launcher lifecycle — npm run cli:pack succeeds but the README never says how to run the CLI; cli/dist/cli.js does not exist (actual entry is cli/cli.js), and the launcher parent gets SIGKILLed under a timeout wrapper whil

## Dogfood Findings (2026-09-07)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"HTTP server: Next.js 16 app (plain JS ESM) exposing an OpenAI-compatible /v1 API (chat/completions, messages, models, etc.) plus a web dashboard at http://localhost:20128/dashboard, booted through custom-server.js (federation-aware wrapper); optional npm CLI launcher in cli/ (upstrea

- [P1] 'OpenCode Free: Ready' is misleading — the free tier 401s upstream without credentials — Registry (open-sse/providers/registry/opencode.js) marks opencode as noAuth:true/category:free, and providers/page.js renders a green 'Ready' badge for all noAuth providers (line 711). Live probe: ope
- [P1] /v1/models advertises ~1021 models that 404 on chat — no credential filtering for fresh installs — buildModelsList (src/app/api/v1/models/route.js:309-343) returns the FULL static catalog when connections.length===0, while src/sse/handlers/chat.js:239-240 returns 404 'No active credentials for prov
- [P1] Login page default-password hint (123456) contradicts .env.example (INITIAL_PASSWORD=change-me) — src/app/login/page.js:254 hardcodes 'Default password is 123456' while dashboardSession.js:80 and login/route.js:58 use process.env.INITIAL_PASSWORD || '123456'. With the documented .env, the page hin
- [P1] DATA_DIR silent fallback to ~/.9router can share/overwrite an existing install's data — src/lib/dataDir.mjs:25-34: unwritable DATA_DIR (e.g. /var/lib/9router from .env.example) logs a warn and falls back to ~/.9router. On a machine with an existing install this silently points login/keys
- [P1] docker compose up -d fails out of the box — headroom sidecar hardcodes port 8787 with no override — docker-compose.yml maps headroom '8787:8787' with no port override; on this host 8787 is occupied (python3 pid 7783) and compose aborts with a raw docker daemon error. The documented compose path (run
\n## Dogfood Findings (2026-09-13)\nVerdict: DOES-NOT-DELIVER\nPromise: {"entry_point":"Local HTTP gateway and Next.js dashboard on http://localhost:20128 (OpenAI-compatible API at /v1, Anthropic-compatible API at /v1/messages, dashboard at /dashboard), launched by the 9router CLI (cli/cli.js), custom-server.js via npm start, or Docker Compose.","promise":"Promise: this\n\n- [P0] Advertised free routes produced no usable completion — Both advertised OpenCode Free models returned HTTP 200 with empty assistant content in source, Docker, and published-CLI runs; the Anthropic-compatible path likewise returned an empty message. Gateway\n- [P1] Model discovery presents unusable routes as available — GET /v1/models exposed 635 models without identifying configured or routable entries; the first listed model failed with no active credentials, preventing a client from reliably selecting a working mo\n- [P1] Documented source quickstart fails under normal user permissions — After npm ci, the copied .env selected DATA_DIR=/var/lib/9router, which was not writable by the unprivileged test user. Startup briefly printed Ready before terminating; a manual writable DATA_DIR ove\n- [P1] Authentication behavior contradicts the documentation — The documented POST /api/keys example returned 401 because it omitted dashboard authentication, while documented source defaults set REQUIRE_API_KEY=false and accepted both missing and bogus consumer \n- [P1] Federation works, but does not rescue the core value claim — A central node and two edges started successfully, and a key created through edge A replicated to central and both edges with revisionLag 0. However, the run provided no successful model completion or
\n## Dogfood Findings (2026-09-13)\nVerdict: PROMISING-BUT-ROUGH\nPromise: {"entry_point":"Local HTTP gateway and Next.js dashboard on port 20128 (/v1 and /dashboard), launched by the 9router CLI or, from source, custom-server.js through the npm scripts.","promise":"Promise: this project claims a user can connect AI coding tools to 40+ providers and 100+ models through one\n\n- [P0] OpenAI-compatible non-streaming chat returns empty completions — Both models advertised by a fresh instance repeatedly returned HTTP 200 with empty assistant content; only streaming requests produced DOGFOOD_OK/PROD_OK, breaking a core compatibility path.n- [P1] Documented source commands fail for unprivileged users — .env.example sets DATA_DIR=/var/lib/9router, causing dev to exit with EACCES and production build to fail during page-data collection; both worked only after overriding DATA_DIR to writable scratch pa\n- [P1] Streaming responses omit the documented completion terminator — Successful streaming calls returned the expected model content but never emitted data: [DONE], which can leave compatible clients unable to detect normal completion.\n- [P1] API bootstrap and authentication documentation contradict runtime behavior — The documented POST /api/keys curl returned 401 without dashboard login and a JWT cookie, while GET /v1/models returned 200 without an API key under REQUIRE_API_KEY=false despite documentation saying \n- [P1] The broad routing and outage-resilience promise remains unproven — The exercise proved a healthy gateway/dashboard, API-key creation, and streaming through two OpenCode Free models in standalone mode; it did not demonstrate 40+ providers, 100+ usable models, automati
\n## Dogfood Findings (2026-09-13)\nVerdict: DOES-NOT-DELIVER\nPromise: {"entry_point":"Primary: HTTP server and Next.js dashboard on port 20128, exposing /v1, /v1/messages, /api, and /dashboard; launched by the 9router CLI or custom-server.js via npm run start. It is not primarily a library, MCP server, or cron job.","promise":"Promise: this project claims a user can k\n\n- [P0] Core OpenAI and Anthropic completion paths fail — OpenAI non-streaming returned HTTP 200 with empty assistant content in three consecutive attempts; Anthropic /v1/messages returned HTTP 200 with no content in both streaming and non-streaming modes. O\n- [P1] Documented source onboarding fails for unprivileged users — After copying .env.example, DATA_DIR=/var/lib/9router caused EACCES during production page-data collection and npm run cli:pack; both required an undocumented writable DATA_DIR override.\n- [P1] CLI reports success when startup has failed — With port 20128 occupied, the published CLI claimed the server was reachable and exited 0 after its child failed with EADDRINUSE, making process status untrustworthy.\n- [P1] Authentication documentation contradicts runtime behavior — The documented POST /api/keys example returned 401 without an undocumented dashboard login cookie, while source /v1/models, keyless chat, and /api/federation/status returned 200 despite documentation \n- [P2] A narrow working path proves real but limited value — Time to first success was 43 seconds; the published package served health, dashboard login and API-key round-trip worked, and an active free OpenCode model returned dogfood-ok through OpenAI streaming

## Dogfood Findings (2026-09-16)
Verdict: PROMISING-BUT-ROUGH
Promise: {"entry_point":"Both: a Next.js 16 HTTP server/API (package '9router-app', server entrypoint custom-server.js via 'npm run start', routes src/app/api/v1/* rewritten from /v1/*, dashboard at /dashboard, federation API at /api/federation/{snapshot,delta,verify,status,replay}) plus a separate npm CLI l

- [P0] Streaming completions are broken against the one credential-free provider path — Every stream:true call to the connected ollama-local provider returned 503 {"error":{"message":"[ollama-local/gemma3:4b] Provider error (reset after 30s)"}} while Ollama itself answered 200; server st
- [P0] /v1/messages returns an OpenAI body, so Anthropic clients cannot parse it — An Anthropic-format request (also with x-api-key + anthropic-version headers) came back as object:"chat.completion" with OpenAI usage shape instead of an Anthropic message object. Translation is verif
- [P1] Usage/quota tracking records nothing despite successful traffic — Server logged '📊 DONE ... IN 0 · OUT 0' across 4 successful completions; usageHistory/usageDaily/requestDetails stayed at 0 rows in the scratch SQLite DB and /api/usage/stats was all zeros. The READM
- [P1] FEDERATION.md's documented commands do not reproduce, and the example compose stack cannot coexist with an existing 9router — The documented central check curl http://localhost:20128/api/federation/status returned 401 with the repo's own .env token (running container had a different token, nothing diagnoses the mismatch), an
- [P1] Quick-start port handling contradicts itself and the shipped defaults collide on a normal host — The app honors PORT but the CLI launcher ignores it and hardcodes 20128, so the documented 'node cli/cli.js --skip-update --no-browser' fails with EADDRINUSE on any machine already running 9router (re
